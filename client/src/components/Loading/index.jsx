import React from 'react';
import { LoadingContainer, Spinner } from './loadingElements';

const Loading = () => {
  return (
    <LoadingContainer>
      <Spinner />
    </LoadingContainer>
  );
};

export default Loading;
